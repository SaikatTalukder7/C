#include <stdio.h>

int main() {
    int num1,num2;
    scanf("%d %d",&num1,&num2);
    int sum = num1+num2;
    if(sum%2==0){
        printf("yes");
    }
    else
    {
       printf("no");
    }

    return 0;
}
